interface:
  display_name: "beacon-release"
  short_description: "beacon 1+6 public release core — modes in-package"
  default_prompt: |
    ${beacon-release}
    Surface: release
    Project: .
    Version: auto
    1) Pick mode_id from references/modes/INDEX.md
    2) Read that mode file fully
    3) Follow HARD GATE; emit mode_id + recommended_next_harness
