interface:
  display_name: "Beacon PRD Bootstrap"
  short_description: "Guide for beacon prd bootstrap workflows"
  default_prompt: "Use $beacon-gen-truth-init to run the beacon prd bootstrap workflow with the current Beacon context."
