interface:
  display_name: "beacon-goal"
  short_description: "beacon 1+6 public goal core — modes in-package"
  default_prompt: |
    ${beacon-goal}
    Surface: goal
    Project: .
    Version: auto
    1) Pick mode_id from references/modes/INDEX.md
    2) Read that mode file fully
    3) Follow HARD GATE; emit mode_id + recommended_next_harness
