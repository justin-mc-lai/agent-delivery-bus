# Research: search-boundary-curation

## 结论

ADB 只做边界提案的待审与拍板；网络搜索/整理在 Hermes 脚本或 skill。定时触发复用 vision-flywheel schedule。

## 选项

| 选项 | 取舍 |
|------|------|
| A. ADB 内嵌搜索 | 超纲，拒绝 |
| B. Hermes script → ingest → decide | 默认，本湖 |
| C. 自动 approve | 拒绝（用户主权） |
