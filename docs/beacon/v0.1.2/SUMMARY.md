# Beacon v0.1.2 Summary

## 版本定位

session-routing：聊天会话稳定调度。四轴会话身份 + SessionRegistry + envelope v1.1 + intent --agent + Session context 注入 + reconcile 回发 + 审批渠道身份。

## 特性包

- [session-routing](features/session-routing/truth.md)

## 边界

- 渠道入站 webhook 自动化、跨设备同步不在本版本；release 人工门。
